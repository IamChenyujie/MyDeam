<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chen.myapplication">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 相机 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 振动 -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mvp.view.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvp.view.activity.HomeActivity" />
        <activity android:name=".mvp.view.activity.SoushuoActivity" />
        <activity android:name=".mvp.view.activity.LeibiaoActivity" />
        <activity android:name=".mvp.view.activity.XiaoqingActivity" />
        <activity android:name=".mvp.view.activity.XiaoqingsActivity" />
        <activity android:name=".mvp.view.activity.ShaomiaoActivity"/>
        <activity android:name=".mvp.view.activity.ShaomiaosActivity"/>
    </application>

</manifest>